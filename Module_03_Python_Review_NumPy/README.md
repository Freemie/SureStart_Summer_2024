# NumPy

## Topics covered in today's module

* Introduction to NumPy
* Arrays/Matrices
* Matrix operations

## Main takeaways from doing today's assignment
- Proficient use of NumPy for creating and manipulating n-dimensional arrays.
- Understood broadcasting rules for array operations.
- Mastered slicing, subsetting, and reshaping of arrays.
- Implemented matrix operations: multiplication, inverse, and determinant.
- Explored how NumPy integrates into deep learning workflows (e.g., few-shot learning and super-resolution).
- Learned to write reproducible code and organize notebooks with a Table of Contents.

## Challenging, interesting, or exciting aspects of today's assignment
- `np.dot()` vs element-wise multiplication when multiplying rows with vectors.
- Mistaken use of `np.arrange()` instead of the correct `np.arange()`.
- Indexing confusion due to misunderstanding 0-based indexing (common when transitioning from R).
- Differentiating between `np.tile()` and `np.repeat()`.
- Reshaping arrays for multi-dimensional operations (e.g., converting a `(3,2,3)` array into `(2,3,3)` using `np.swapaxes` or `np.transpose`).

## Additional resources used 
W3schools
